export interface Token {
    tokenName: string;
    chainId: string;
    // symbol: string;
}

export interface PriceInfo {
    assetTokenAddress: string;
    chainId: string;
    // priceUsd: string;
}